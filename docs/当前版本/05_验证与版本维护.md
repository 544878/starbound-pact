# 验证与版本维护

[返回文档目录](../README.md)

## 当前证据与边界

2026-09-11整理时确认：主页面引用V2战斗页面，V2页面调用`src/systems/v2/battle.ts`；存档格式常量为10；设计配置包含51名角色、51件角色专武、8位Boss和51个基础遭遇。分类生成器核对设计源与运行配置中的SHA256，避免不同版本混排。

V2数学模块使用统一属性点数，成长为`0.4 + 0.6 × (等级−1)/89`；暴伤字段表示额外暴伤。行动间隔为`10000/速度`，初始战技点为4。旧数值档案中的面板除以250、初始3点、暴伤总倍率等口径不能直接用于V2。

根目录设计源末尾和[原始规则验证记录](../历史档案/04_规则验证与验收.md)记录的是设计阶段验证。当时的167,151条断言不是战斗场次，也不是本次执行结果。原文关于“尚无完整可玩实现”的表述属于当时状态；如今主战斗已有V2入口，但是否完整实现仍须逐机制验收。

本次是文档重写与分类整理，不重新调整数值，也不把四星C6≈五星0+1等平衡目标标为已经全面达成。当前战斗、外围活动和Unity的规则范围要分别验证。

## 本次整理实测（2026-09-11）

- V2导出成功：51名角色、8位Boss、51个遭遇；Markdown与HTML角色技能文本一致。
- 分类文档来源SHA256与运行配置一致；51名角色完整正文均保留。
- 8份入口及分类文档共298个本地链接与角色锚点通过检查。
- `npm test -- src/systems/v2`：3个测试文件、122项测试通过。此结果限于V2测试范围，不代表全项目或长期实战平衡验收。
- 两个历史导出脚本通过语法检查；本次未重新生成旧数值快照，保留迁移前原文。

## 更新步骤

1. 修改根目录`星契V2_完整重构设计.md`及对应HTML，保持角色技能文本一致。
2. 运行`node scripts/export-v2-design.mjs`，校验覆盖与文本一致性并更新运行配置。
3. 运行`node scripts/export-current-docs.mjs`，生成数值、角色、敌人三份分类阅读版。
4. 涉及行为变更时同步修改V2实现并运行对应测试；涉及项目范围或存档时手动更新项目说明。

分类阅读版不直接手改；角色通过稳定ID关联，名字相似、星级相同或体系相同都不代表同一角色。设计内容与行为代码冲突时记录差异，再修正源头，不能借整理文档悄悄改变玩法。

## 历史导出

`export-current-balance.mjs`读取旧阵法与角色结算模块，虽然名称含current，也不能作为V2全局数值导出。它和`export-four-star.mjs`现统一输出到`docs/历史档案/`。保留这些脚本是为了核对旧机制与活动，不用于覆盖当前V2角色图鉴。
