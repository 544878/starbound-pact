# 当前页素材盘点

范围仅限用户截图的 10 位角色：露弥、艾尔登、瑟琳、米菈、诺克缇娅、凯尔、星璃、玄照、沧澜、炎煌。

用户要求先检查已有资源，已停止图片生成。不要自动重新生成整套素材。

- 原有专武：10 位均有文件，位于 `public/assets/weapons/`。
- 本轮透明独立素材：18 张，位于 `public/assets/signature-v2/`。露弥、艾尔登、瑟琳、米菈各 4 张；诺克缇娅为专武、真身 2 张。
- 重复生成的浅底备选：9 张，位于 `public/assets/signature-v3/`。不用于深色界面，当前代码已取消该目录的引用。
- 缓存额外 4 张：3 张瑟琳备选与 1 张误带人物的艾尔登专武，均非缺失角色的新素材，不接入。
- 按独立装备槽位计，已有 23/40 个文件可对应（含 5 件原有专武）；仍缺 17 个独立星魂槽位。缺失部分当前保留原共用星魂图，不冒充已完成重构。

白色方块的原因是展示层误用了不透明浅底图，并非旧透明图丢失。已恢复透明资源引用，保留所有已有文件。透明原图个别细边仍有彩边瑕疵，不能宣称已通过最终美术质量验收。

`existing-audit.json` 保存文件尺寸、透明范围和 SHA-256，用于识别重复文件。`coverage.json` 为当前槽位覆盖检查。后续操作须先核对这两份清单以及现有文件，再决定是否需要补图。

验证：浏览器检查露弥后三魂逐项切换、露弥到艾尔登专武切换及手机显示；截图位于本目录。生产构建通过。全量 40 张美术重构未完成。
